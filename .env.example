# Vite reads env vars prefixed with VITE_ at build time.
# Left empty on purpose: the dev server proxies /api to localhost:8000
# (see vite.config.ts), and the Docker nginx config proxies /api to the
# backend service. No API base URL needs to be baked in for either case.
